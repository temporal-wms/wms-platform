apiVersion: apps/v1
kind: Deployment
metadata:
  name: wes-service
  namespace: wms-platform
  labels:
    app: wes-service
    tier: backend
    component: microservice
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: wes-service
  template:
    metadata:
      labels:
        app: wes-service
        tier: backend
        component: microservice
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8016"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: wms-platform
      containers:
      - name: wes-service
        image: wms-platform/wes-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8016
          protocol: TCP
        env:
        - name: SERVICE_NAME
          value: "wes-service"
        - name: LOG_LEVEL
          value: "info"
        - name: SERVER_ADDR
          value: ":8016"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: mongodb-credentials
              key: uri
        - name: MONGODB_DATABASE
          value: "wes_db"
        - name: KAFKA_BROKERS
          value: "kafka-0.kafka-headless.wms-platform.svc.cluster.local:9092,kafka-1.kafka-headless.wms-platform.svc.cluster.local:9092,kafka-2.kafka-headless.wms-platform.svc.cluster.local:9092"
        - name: PROCESS_PATH_SERVICE_URL
          value: "http://process-path-service.wms-platform.svc.cluster.local:8015"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://jaeger-collector.observability.svc.cluster.local:4317"
        - name: TRACING_ENABLED
          value: "true"
        - name: ENVIRONMENT
          value: "development"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 0
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
      terminationGracePeriodSeconds: 30
